/*
Deployment script for DB_A4753D_university

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "DB_A4753D_university"
:setvar DefaultFilePrefix "DB_A4753D_university"
:setvar DefaultDataPath "H:\Program Files\Microsoft SQL Server\MSSQL14.MSSQLSERVER\MSSQL\Data\"
:setvar DefaultLogPath "H:\Program Files\Microsoft SQL Server\MSSQL14.MSSQLSERVER\MSSQL\Data\"

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
/*
The column [dbo].[teacher_subject].[Subject_id] on table [dbo].[teacher_subject] must be added, but the column has no default value and does not allow NULL values. If the table contains data, the ALTER script will not work. To avoid this issue you must either: add a default value to the column, mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.
*/

IF EXISTS (select top 1 1 from [dbo].[teacher_subject])
    RAISERROR (N'Rows were detected. The schema update is terminating because data loss might occur.', 16, 127) WITH NOWAIT

GO
PRINT N'Dropping unnamed constraint on [dbo].[students]...';


GO
ALTER TABLE [dbo].[students] DROP CONSTRAINT [FK__students__Course__47DBAE45];


GO
PRINT N'Dropping unnamed constraint on [dbo].[student_subject]...';


GO
ALTER TABLE [dbo].[student_subject] DROP CONSTRAINT [FK__student_s__Stude__46E78A0C];


GO
PRINT N'Dropping unnamed constraint on [dbo].[student_subject]...';


GO
ALTER TABLE [dbo].[student_subject] DROP CONSTRAINT [FK__student_s__Subje__4BAC3F29];


GO
PRINT N'Dropping unnamed constraint on [dbo].[teacher_subject]...';


GO
ALTER TABLE [dbo].[teacher_subject] DROP CONSTRAINT [FK__teacher_s__Teach__48CFD27E];


GO
PRINT N'Starting rebuilding table [dbo].[course]...';


GO
BEGIN TRANSACTION;

SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;

SET XACT_ABORT ON;

CREATE TABLE [dbo].[tmp_ms_xx_course] (
    [Id]       INT           IDENTITY (1, 1) NOT NULL,
    [Name]     NVARCHAR (10) NOT NULL,
    [Birthday] DATE          NOT NULL,
    PRIMARY KEY CLUSTERED ([Id] ASC)
);

IF EXISTS (SELECT TOP 1 1 
           FROM   [dbo].[course])
    BEGIN
        SET IDENTITY_INSERT [dbo].[tmp_ms_xx_course] ON;
        INSERT INTO [dbo].[tmp_ms_xx_course] ([Id], [Name], [Birthday])
        SELECT   [Id],
                 [Name],
                 [Birthday]
        FROM     [dbo].[course]
        ORDER BY [Id] ASC;
        SET IDENTITY_INSERT [dbo].[tmp_ms_xx_course] OFF;
    END

DROP TABLE [dbo].[course];

EXECUTE sp_rename N'[dbo].[tmp_ms_xx_course]', N'course';

COMMIT TRANSACTION;

SET TRANSACTION ISOLATION LEVEL READ COMMITTED;


GO
PRINT N'Starting rebuilding table [dbo].[student_subject]...';


GO
BEGIN TRANSACTION;

SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;

SET XACT_ABORT ON;

CREATE TABLE [dbo].[tmp_ms_xx_student_subject] (
    [Id]          INT IDENTITY (1, 1) NOT NULL,
    [Student_Id]  INT NOT NULL,
    [Subjects_Id] INT NOT NULL,
    [Mark]        INT NULL,
    PRIMARY KEY CLUSTERED ([Id] ASC)
);

IF EXISTS (SELECT TOP 1 1 
           FROM   [dbo].[student_subject])
    BEGIN
        SET IDENTITY_INSERT [dbo].[tmp_ms_xx_student_subject] ON;
        INSERT INTO [dbo].[tmp_ms_xx_student_subject] ([Id], [Student_Id], [Subjects_Id])
        SELECT   [Id],
                 [Student_Id],
                 [Subjects_Id]
        FROM     [dbo].[student_subject]
        ORDER BY [Id] ASC;
        SET IDENTITY_INSERT [dbo].[tmp_ms_xx_student_subject] OFF;
    END

DROP TABLE [dbo].[student_subject];

EXECUTE sp_rename N'[dbo].[tmp_ms_xx_student_subject]', N'student_subject';

COMMIT TRANSACTION;

SET TRANSACTION ISOLATION LEVEL READ COMMITTED;


GO
PRINT N'Starting rebuilding table [dbo].[students]...';


GO
BEGIN TRANSACTION;

SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;

SET XACT_ABORT ON;

CREATE TABLE [dbo].[tmp_ms_xx_students] (
    [Id]         INT           IDENTITY (1, 1) NOT NULL,
    [FirstName]  NVARCHAR (20) NOT NULL,
    [LastName]   NVARCHAR (20) NOT NULL,
    [MiddleName] NVARCHAR (20) NOT NULL,
    [Birthday]   DATETIME      NOT NULL,
    [Address]    NVARCHAR (30) NULL,
    [Phone]      VARCHAR (15)  NULL,
    [Course_Id]  INT           NOT NULL,
    PRIMARY KEY CLUSTERED ([Id] ASC)
);

IF EXISTS (SELECT TOP 1 1 
           FROM   [dbo].[students])
    BEGIN
        SET IDENTITY_INSERT [dbo].[tmp_ms_xx_students] ON;
        INSERT INTO [dbo].[tmp_ms_xx_students] ([Id], [FirstName], [LastName], [MiddleName], [Birthday], [Address], [Phone], [Course_Id])
        SELECT   [Id],
                 [FirstName],
                 [LastName],
                 [MiddleName],
                 [Birthday],
                 [Address],
                 [Phone],
                 [Course_Id]
        FROM     [dbo].[students]
        ORDER BY [Id] ASC;
        SET IDENTITY_INSERT [dbo].[tmp_ms_xx_students] OFF;
    END

DROP TABLE [dbo].[students];

EXECUTE sp_rename N'[dbo].[tmp_ms_xx_students]', N'students';

COMMIT TRANSACTION;

SET TRANSACTION ISOLATION LEVEL READ COMMITTED;


GO
PRINT N'Starting rebuilding table [dbo].[subjects]...';


GO
BEGIN TRANSACTION;

SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;

SET XACT_ABORT ON;

CREATE TABLE [dbo].[tmp_ms_xx_subjects] (
    [Id]   INT           IDENTITY (1, 1) NOT NULL,
    [Name] NVARCHAR (15) NOT NULL,
    PRIMARY KEY CLUSTERED ([Id] ASC)
);

IF EXISTS (SELECT TOP 1 1 
           FROM   [dbo].[subjects])
    BEGIN
        SET IDENTITY_INSERT [dbo].[tmp_ms_xx_subjects] ON;
        INSERT INTO [dbo].[tmp_ms_xx_subjects] ([Id], [Name])
        SELECT   [Id],
                 [Name]
        FROM     [dbo].[subjects]
        ORDER BY [Id] ASC;
        SET IDENTITY_INSERT [dbo].[tmp_ms_xx_subjects] OFF;
    END

DROP TABLE [dbo].[subjects];

EXECUTE sp_rename N'[dbo].[tmp_ms_xx_subjects]', N'subjects';

COMMIT TRANSACTION;

SET TRANSACTION ISOLATION LEVEL READ COMMITTED;


GO
PRINT N'Starting rebuilding table [dbo].[teacher_subject]...';


GO
BEGIN TRANSACTION;

SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;

SET XACT_ABORT ON;

CREATE TABLE [dbo].[tmp_ms_xx_teacher_subject] (
    [Id]         INT IDENTITY (1, 1) NOT NULL,
    [Teacher_id] INT NOT NULL,
    [Subject_id] INT NOT NULL,
    PRIMARY KEY CLUSTERED ([Id] ASC)
);

IF EXISTS (SELECT TOP 1 1 
           FROM   [dbo].[teacher_subject])
    BEGIN
        SET IDENTITY_INSERT [dbo].[tmp_ms_xx_teacher_subject] ON;
        INSERT INTO [dbo].[tmp_ms_xx_teacher_subject] ([Id], [Teacher_id])
        SELECT   [Id],
                 [Teacher_id]
        FROM     [dbo].[teacher_subject]
        ORDER BY [Id] ASC;
        SET IDENTITY_INSERT [dbo].[tmp_ms_xx_teacher_subject] OFF;
    END

DROP TABLE [dbo].[teacher_subject];

EXECUTE sp_rename N'[dbo].[tmp_ms_xx_teacher_subject]', N'teacher_subject';

COMMIT TRANSACTION;

SET TRANSACTION ISOLATION LEVEL READ COMMITTED;


GO
PRINT N'Starting rebuilding table [dbo].[teachers]...';


GO
BEGIN TRANSACTION;

SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;

SET XACT_ABORT ON;

CREATE TABLE [dbo].[tmp_ms_xx_teachers] (
    [Id]         INT           IDENTITY (1, 1) NOT NULL,
    [FirstName]  NVARCHAR (20) NOT NULL,
    [LastName]   NVARCHAR (20) NOT NULL,
    [MiddleName] NVARCHAR (20) NOT NULL,
    [Birthday]   DATE          NOT NULL,
    [Address]    NVARCHAR (30) NULL,
    [Phone]      VARCHAR (15)  NULL,
    PRIMARY KEY CLUSTERED ([Id] ASC)
);

IF EXISTS (SELECT TOP 1 1 
           FROM   [dbo].[teachers])
    BEGIN
        SET IDENTITY_INSERT [dbo].[tmp_ms_xx_teachers] ON;
        INSERT INTO [dbo].[tmp_ms_xx_teachers] ([Id], [FirstName], [LastName], [MiddleName], [Birthday], [Address], [Phone])
        SELECT   [Id],
                 [FirstName],
                 [LastName],
                 [MiddleName],
                 [Birthday],
                 [Address],
                 [Phone]
        FROM     [dbo].[teachers]
        ORDER BY [Id] ASC;
        SET IDENTITY_INSERT [dbo].[tmp_ms_xx_teachers] OFF;
    END

DROP TABLE [dbo].[teachers];

EXECUTE sp_rename N'[dbo].[tmp_ms_xx_teachers]', N'teachers';

COMMIT TRANSACTION;

SET TRANSACTION ISOLATION LEVEL READ COMMITTED;


GO
PRINT N'Creating unnamed constraint on [dbo].[students]...';


GO
ALTER TABLE [dbo].[students] WITH NOCHECK
    ADD FOREIGN KEY ([Course_Id]) REFERENCES [dbo].[course] ([Id]);


GO
PRINT N'Creating unnamed constraint on [dbo].[student_subject]...';


GO
ALTER TABLE [dbo].[student_subject] WITH NOCHECK
    ADD FOREIGN KEY ([Student_Id]) REFERENCES [dbo].[students] ([Id]);


GO
PRINT N'Creating unnamed constraint on [dbo].[student_subject]...';


GO
ALTER TABLE [dbo].[student_subject] WITH NOCHECK
    ADD FOREIGN KEY ([Subjects_Id]) REFERENCES [dbo].[subjects] ([Id]);


GO
PRINT N'Creating unnamed constraint on [dbo].[teacher_subject]...';


GO
ALTER TABLE [dbo].[teacher_subject] WITH NOCHECK
    ADD FOREIGN KEY ([Teacher_id]) REFERENCES [dbo].[teachers] ([Id]);


GO
PRINT N'Creating unnamed constraint on [dbo].[teacher_subject]...';


GO
ALTER TABLE [dbo].[teacher_subject] WITH NOCHECK
    ADD FOREIGN KEY ([Subject_id]) REFERENCES [dbo].[subjects] ([Id]);


GO
PRINT N'Checking existing data against newly created constraints';


GO
USE [$(DatabaseName)];


GO
CREATE TABLE [#__checkStatus] (
    id           INT            IDENTITY (1, 1) PRIMARY KEY CLUSTERED,
    [Schema]     NVARCHAR (256),
    [Table]      NVARCHAR (256),
    [Constraint] NVARCHAR (256)
);

SET NOCOUNT ON;

DECLARE tableconstraintnames CURSOR LOCAL FORWARD_ONLY
    FOR SELECT SCHEMA_NAME([schema_id]),
               OBJECT_NAME([parent_object_id]),
               [name],
               0
        FROM   [sys].[objects]
        WHERE  [parent_object_id] IN (OBJECT_ID(N'dbo.students'), OBJECT_ID(N'dbo.student_subject'), OBJECT_ID(N'dbo.teacher_subject'))
               AND [type] IN (N'F', N'C')
                   AND [object_id] IN (SELECT [object_id]
                                       FROM   [sys].[check_constraints]
                                       WHERE  [is_not_trusted] <> 0
                                              AND [is_disabled] = 0
                                       UNION
                                       SELECT [object_id]
                                       FROM   [sys].[foreign_keys]
                                       WHERE  [is_not_trusted] <> 0
                                              AND [is_disabled] = 0);

DECLARE @schemaname AS NVARCHAR (256);

DECLARE @tablename AS NVARCHAR (256);

DECLARE @checkname AS NVARCHAR (256);

DECLARE @is_not_trusted AS INT;

DECLARE @statement AS NVARCHAR (1024);

BEGIN TRY
    OPEN tableconstraintnames;
    FETCH tableconstraintnames INTO @schemaname, @tablename, @checkname, @is_not_trusted;
    WHILE @@fetch_status = 0
        BEGIN
            PRINT N'Checking constraint: ' + @checkname + N' [' + @schemaname + N'].[' + @tablename + N']';
            SET @statement = N'ALTER TABLE [' + @schemaname + N'].[' + @tablename + N'] WITH ' + CASE @is_not_trusted WHEN 0 THEN N'CHECK' ELSE N'NOCHECK' END + N' CHECK CONSTRAINT [' + @checkname + N']';
            BEGIN TRY
                EXECUTE [sp_executesql] @statement;
            END TRY
            BEGIN CATCH
                INSERT  [#__checkStatus] ([Schema], [Table], [Constraint])
                VALUES                  (@schemaname, @tablename, @checkname);
            END CATCH
            FETCH tableconstraintnames INTO @schemaname, @tablename, @checkname, @is_not_trusted;
        END
END TRY
BEGIN CATCH
    PRINT ERROR_MESSAGE();
END CATCH

IF CURSOR_STATUS(N'LOCAL', N'tableconstraintnames') >= 0
    CLOSE tableconstraintnames;

IF CURSOR_STATUS(N'LOCAL', N'tableconstraintnames') = -1
    DEALLOCATE tableconstraintnames;

SELECT N'Constraint verification failed:' + [Schema] + N'.' + [Table] + N',' + [Constraint]
FROM   [#__checkStatus];

IF @@ROWCOUNT > 0
    BEGIN
        DROP TABLE [#__checkStatus];
        RAISERROR (N'An error occurred while verifying constraints', 16, 127);
    END

SET NOCOUNT OFF;

DROP TABLE [#__checkStatus];


GO
PRINT N'Update complete.';


GO
